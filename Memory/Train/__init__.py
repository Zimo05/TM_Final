"""Training and inference entry points for the Hawkes Memory Tree.

Import concrete APIs from ``Train.Train`` and ``Train.Inference``. Keeping the
package initializer lightweight also allows both modules to run cleanly with
``python -m``.
"""
